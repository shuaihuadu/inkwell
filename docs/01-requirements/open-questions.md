---
id: open-questions-custom-agent
stage: H1
status: pending
authors:
  - name: H1-RequirementsInterviewer
    role: agent
reviewers: []
created: 2026-05-06
updated: 2026-05-06
upstream:
  - requirements-custom-agent
downstream: []
---

# 自定义 Agent 功能 · H1 待澄清清单

> 首轮 15 条 OQ 已于 2026-05-06 第二轮访谈中关闭；同日第三轮访谈因客户硬约束要求**重启** OQ-001（范围扩大）与 OQ-019（知识库是否仍为不做），并新增 OQ-020 ~ OQ-029。本轮决策后，OQ-001 与 OQ-020 ~ OQ-029 都已 closed；OQ-019 重置为 `pending`（作为 vNext / pending 生活）。
>
> **2026-05-06 H1 原型评审会** 又新增 **OQ-030**（试运行下半屏落地形态：同页 React 组件 vs iframe 接入），标 `non-blocking`，留给 H2 决策。原则上本文件不再接收新条目；若后续阶段发现新问题，走新文件 `open-questions-v2.md` 或新需求文件。

## 字段说明

- **影响范围**：列出哪些 REQ / NFR / PB / DB / 异常 / 风险会被这条 OQ 的回答改写。
- **候选答**：列出**有限可选项**让用户挑，不替用户拍默认值。如果用户答案不在候选里，请在评审中明确补充。
- **卡点等级**：
  - `blocking` = 不答这条，对应 REQ 无法进入 MVP / H2。
  - `non-blocking` = 不答这条，可继续，但会在 H2/H3 阶段以风险形式继续追跟。
  - `closed YYYY-MM-DD` = 已在评审中关闭，回写位置见每条 OQ 末尾的"回写"行。

---

## OQ-001 MVP 切分：7 项能力先上哪 1–2 项？

选择A

> **re-opened 2026-05-06**：客户在第三轮访谈中硬约束要求“务必要实现 Tools、MCP 和 Skill 配置”；范围改为 REQ-001 ~ REQ-004 + REQ-010 + REQ-011 + REQ-012（UI 占位），详见 [OQ-021](#oq-021-客户项-将-skill--tool--mcp-从-vnext-提升到-mvp-并接受交付周期调整)。

- **问题**：用户在结构化问询中**全选**了 7 项能力（元数据 / 角色 prompt / 模型与参数 / 工具与知识库 / 版本与导入导出 / 共享与权限 / 运行时观测），单次 H1 无法落地全部。需要明确 MVP 范围。
- **为什么需要答**：决定 H1 → H2 进门时实际开建的 REQ 集合，以及 H2 架构覆盖面。
- **影响范围**：REQ-001 ~ REQ-009 的"是否进入 MVP"、风险 R1。
- **候选答（请挑一组，可补注释）**：
  - A. MVP 只做 **REQ-001（生命周期）+ REQ-002（元数据）+ REQ-003（角色 Prompt）+ REQ-004（试运行）**，其余全部 vNext。
  - B. 在 A 基础上加 **REQ-008（共享与权限，仅"私有 + 已登录用户可见的公开"两档）**。
  - C. 在 A 基础上加 **REQ-007（仅导入，不导出）**，且导入仅支持本平台导出的 JSON。
  - D. 其他组合（请显式列出，并附 OQ-002 的取舍）。
- **卡点等级**：closed 2026-05-06
- **回写**：requirements.md 第 5 节 REQ-001~004 标 MVP；REQ-005~008 标 vNext；REQ-009 撤销并入 ND-007。

## OQ-002 "非技术用户"与"模型参数 / 工具 / MCP"的 UX 取舍

选择A

- **问题**：temperature、top_p、MCP server endpoint、工具 schema、知识库索引这些天然是开发者概念。"非技术终端用户"作为目标用户群，与这些能力存在显著 UX 张力。需要选择一条取舍策略。
- **为什么需要答**：直接决定 REQ-005 / REQ-006 是降级（模板向导）/ 全保留 / 砍掉，并改写 NFR-001 的具体可检查项。
- **影响范围**：REQ-005、REQ-006、NFR-001、风险 R2。
- **候选答（请挑一项）**：
  - A. **模板 / 向导型 UX**：参数与挂载点全部包装成业务化预设（如"快/准/创意"、"接入我的产品资料库"），不向终端用户暴露 temperature / MCP transport 等术语。
  - B. **全保留**：相信用户能自学，UI 直接暴露开发者概念，并在帮助文档兜底。
  - C. **MVP 砍除**：本期完全不做参数与工具挂载，等"高级用户"群体明确后再加；与 OQ-001 联动。
  - D. **分层**：MVP 给 A，"高级用户"开关下进入 B（需要在本期就规划开关入口）。
- **卡点等级**：closed 2026-05-06
- **回写**：requirements.md REQ-005 / REQ-006 vNext 锚定；NFR-001。

## OQ-003 "团队"是否纳入 MVP；团队的承载实体是什么？

选择A

- **问题**："私有 / 团队 / 公开"中的"团队"未定义。本平台是否已存在企业 / 项目 / 自建组等组织实体？团队成员的引入路径是什么？
- **为什么需要答**：决定 REQ-008 是否需要在 MVP 引入"组织 / 团队成员 / 邀请"等数据模型与权限矩阵。
- **影响范围**：REQ-008、PB-OQ-A、用户角色第 3 节、R3。
- **候选答**：
  - A. MVP **不**做团队，只做"私有 + 公开"两档。
  - B. MVP 做团队，团队 = **既有企业账号下的成员**（依赖平台已有组织模型，本草稿不假设其形态）。
  - C. MVP 做团队，团队 = **用户自建小组**，邀请通过链接 / 邮箱。
  - D. 其他。
- **卡点等级**：closed 2026-05-06
- **回写**：requirements.md 第 3 节用户角色仅保留两类；PB-003；REQ-008 vNext 锚定；ND-005。

## OQ-004 "公开"= 任何登录用户可见 还是 互联网匿名可见？

任何登录用户可见

- **问题**：这两套语义在合规面差异巨大——后者牵涉内容审核、反滥用、滥发链接、未成年人保护等持续治理工作。
- **为什么需要答**：决定 REQ-008 的实现深度、是否需要内容审核子系统、是否需要风控告警入口。
- **影响范围**：REQ-008、PB-OQ-B、E4、OQ-012、R3。
- **候选答**：
  - A. 公开 = **仅平台已登录用户可见**（无匿名访问入口）。
  - B. 公开 = **互联网匿名可见**（凡持有链接者皆可访问）。
  - C. 提供两档，作者在分享时选择；MVP 仅启用 A，B 留 vNext。
  - D. MVP 不做"公开"，仅"私有 + 团队"。
- **卡点等级**：closed 2026-05-06
- **回写**：requirements.md PB-004；REQ-008 vNext 锚定；ND-009 / R7 隐含登录依赖。

## OQ-005 运行时观测的范围、保留期、导出策略

选择D

- **问题**：用户提到"评测、日志、追踪"。范围是离线 batch 评测、还是线上抽样追踪？数据保留多久？是否允许作者导出？
- **为什么需要答**：决定 REQ-009 的形态与 H2 阶段的可观测性技术栈选择面。
- **影响范围**：REQ-009、E1、OQ-008（合规面交叉）。
- **候选答**：
  - A. 仅线上抽样追踪（每次调用记录 trace / metric），保留期由 OQ-008 决定。
  - B. 仅离线 batch 评测（作者自定义评测集，按需触发）。
  - C. 二者皆有，MVP 上 A，vNext 上 B。
  - D. 本特性内不做观测，留给平台运维模块。
- **卡点等级**：closed 2026-05-06
- **回写**：requirements.md ND-007；REQ-009 撤销；S7 标"已移除"。

## OQ-006 导入来源与格式标准

只支持本平台 JSON

- **问题**：导入是只支持本平台 JSON，还是必须兼容 OpenAI Assistants / GPTs / Coze / Dify 等外部标准？
- **为什么需要答**：决定 REQ-007 是否提前进入 MVP、决定是否需要长期跟随外部 schema 演进的工程负担。
- **影响范围**：REQ-007、REQ-006（"工具描述"格式）、E2、R6。
- **候选答**：
  - A. 仅本平台导出的 JSON（自有 schema）。
  - B. A + 兼容 **OpenAI Assistants schema** 单一外部标准。
  - C. A + 多外部标准（请列出名单）。
  - D. MVP 不做导入。
- **卡点等级**：closed 2026-05-06
- **回写**：requirements.md REQ-006 / REQ-007 vNext 锚定；E2；R6。

## OQ-007 模型清单、Key 来源、配额与计费

平台托管 Key, Key可以配置到系统的配置文件

- **问题**：用户能选哪些模型？是平台托管 Key 还是用户自带 Key？人均 / 每 Agent / 每对话的配额？计费模型？
- **为什么需要答**：直接决定 REQ-005 是否能在产品 / 商业上跑通，以及 H2 的网关 / 计费架构。
- **影响范围**：REQ-005、E1（超额场景）、R1。
- **候选答**：
  - A. **平台托管 Key**，固定模型清单（请提供首批清单），按用量计费 / 内部分摊。
  - B. **用户自带 Key**，模型清单由用户自配（更靠近开发者用户，与 OQ-002 选 A 冲突）。
  - C. **混合**：平台托管为主，进阶用户可填自有 Key。
- **卡点等级**：closed 2026-05-06
- **回写**：requirements.md REQ-005 vNext 锚定（gpt-4.1 单一模型 / 平台托管 Key 写配置文件 / 每用户每天 N 次 / 按 token 计费）。

## OQ-008 数据合规：用户内容是否用于训练 / 跨域驻留？

平台**绝不**用用户内容训练，数据仅在用户所选地域驻留

- **问题**：用户上传的知识库内容、对话记录、Prompt，是否会被平台用于模型训练 / 评测样本？数据是否存在跨地域驻留要求（如必须留在境内）？
- **为什么需要答**：高敏感问题。本草稿明确**不替用户写默认承诺**——一旦默认错，H6 上线前需要法务介入返工。
- **影响范围**：REQ-006、REQ-009、DB-OQ-A、DB-OQ-B、R5。
- **候选答**：
  - A. 平台**绝不**用用户内容训练，数据仅在用户所选地域驻留。
  - B. 平台**经用户授权后**可用于模型评测 / 改进，且明确告知。
  - C. 由用户在每条 Agent 上自选默认值。
  - D. 其他（请描述并提供法务依据）。
- **卡点等级**：closed 2026-05-06
- **回写**：requirements.md NFR-004 / DB-003；R5。

## OQ-009 分享后版本快照策略

固定快照版

- **问题**：作者把 Agent 分享给别人后，作者改 prompt / 删除 / 撤回，使用者侧的体验是什么？
- **为什么需要答**：决定 REQ-007 的版本字段是否必填、REQ-008 的"撤回"语义、E3 的具体表现。
- **影响范围**：REQ-007、REQ-008、E3、PB-OQ-C、R4。
- **候选答**：
  - A. 分享 = **引用最新版**，作者改即时生效，删除即时失效。
  - B. 分享 = **固定快照版**，使用者侧看到的永远是分享时刻的版本，作者后续改动不影响。
  - C. 由作者在分享动作上选择。
- **卡点等级**：closed 2026-05-06
- **回写**：requirements.md PB-005；REQ-007 / REQ-008 vNext 锚定；E3；R4。

## OQ-010 调用失败 / 超时 / 超额对终端用户的呈现

展示错误并自动重试 N 次，仍失败再展示给用户

- **问题**：模型 / 工具调用失败、超时、超额时，使用者看到什么？是否需要"兜底回复"机制？是否计入计费？
- **为什么需要答**：决定 REQ-004 / E1 的可机械验证条款，以及 H2 是否需要做兜底链路。
- **影响范围**：REQ-004、E1。
- **候选答**：
  - A. 直接展示"出错了，可重试"，不计费。
  - B. 展示错误并自动重试 N 次，仍失败再展示给用户。
  - C. 失败时由平台兜底回复一段固定文案（如"我现在不太方便回答"），并在 Agent 详情侧告知作者。
- **卡点等级**：closed 2026-05-06
- **回写**：requirements.md AC-004-4；E1。

## OQ-011 性能 / 可用性 / 响应时间 SLO 与并发预期

内部小范围

- **问题**：试运行 / 调用的响应时间目标？平台可用性目标？同时在线作者 / 使用者量级？
- **为什么需要答**：H2 架构选型直接依赖这些目标值；不给值会让 H2 凭空挑栈。
- **影响范围**：第 6 节非功能、REQ-004、REQ-009。
- **候选答**：
  - A. 内部小范围（≤100 同时在线），无可用性承诺。
  - B. 部门 / 中等规模（100–10k 同时在线），单机房。
  - C. 公开产品级（10k+），多机房 / 99.9% 可用性。
- **卡点等级**：closed 2026-05-06
- **回写**：requirements.md NFR-002。

## OQ-012 头像 / 公开 Agent 的内容审核策略

不审核

- **问题**：用户上传的头像、Agent 描述、公开 Agent 的角色 Prompt，是否需要审核？事前 / 事后 / 不审核？
- **为什么需要答**：与 OQ-004 强相关。"互联网匿名可见"会把审核要求顶到必做。
- **影响范围**：REQ-002、REQ-008、E4、R3。
- **候选答**：
  - A. 不审核（仅在 OQ-004 选不公开时可行）。
  - B. 事前审核（机器扫描 + 人工复核）。
  - C. 事后审核（举报触发下线）。
  - D. 事前 + 事后双层。
- **卡点等级**：closed 2026-05-06
- **回写**：requirements.md AC-002-7 / AC-003-4；E4；R3（升级为用户已知接受的合规风险）；ND-008。

## OQ-013 数据所有权与离开后归属

软删除并保留 N 天供恢复。

- **问题**：用户注销账号 / 退出团队 / 团队解散时，其名下 / 团队下的 Agent、对话记录、知识库片段如何处理？
- **为什么需要答**：决定 REQ-001 的删除语义边界、DB-OQ-C 的实现路径。
- **影响范围**：REQ-001、REQ-008、DB-OQ-C。
- **候选答**：
  - A. 一并硬删除。
  - B. 软删除并保留 N 天供恢复。
  - C. 团队场景下转给团队管理员；个人场景下硬删除。
  - D. 由用户在离开时选择。
- **卡点等级**：closed 2026-05-06
- **回写**：requirements.md DB-004（N = 7）；AC-001-5；S4。

## OQ-014 名称 / 描述的字符与长度约束

名称 ≤ 50 字符，允许 emoji，允许多语言

- **问题**：Agent 名称长度上限、是否允许 emoji、是否允许多语言混排；描述长度上限。
- **为什么需要答**：影响 REQ-002 的可机械验收边界。
- **影响范围**：REQ-002。
- **候选答**：
  - A. 名称 ≤ 50 字符，允许 emoji，允许多语言。
  - B. 名称 ≤ 30 字符，禁止 emoji。
  - C. 由产品 / 设计同学按品牌规范定。
- **卡点等级**：closed 2026-05-06
- **回写**：requirements.md AC-002-5。

## OQ-015 多语言 / 可访问性 / 端设备覆盖

中英双语 + PC 浏览器 + 移动端响应式，无障碍按等级 AA

- **问题**：是否需要英文 UI？是否需要无障碍（屏幕阅读器、高对比度）？是否覆盖移动端？
- **为什么需要答**：决定 H1 UI 说明阶段必须覆盖的端 / 状态矩阵。
- **影响范围**：第 6 节非功能、第 4 节核心场景。
- **候选答**：
  - A. 仅简体中文 + PC 浏览器，不强制无障碍。
  - B. 中英双语 + PC 浏览器 + 移动端响应式，无障碍按等级 AA。
  - C. 其他（请显式列出）。
- **卡点等级**：closed 2026-05-06
- **回写**：requirements.md NFR-003。

## 关闭流程

每条 OQ 关闭时，请在评审记录里写：

```
OQ-XXX: 选 <候选编号> / 自定义答案
依据: <一句话原因>
回写: requirements.md 的 REQ-NNN / NFR-NNN / E-N 中标记 "OQ-XXX 已关闭" 并补具体条款
```

回写完成后，把本文件中对应 OQ 的 `卡点等级` 字段改为 `closed` 并加上关闭日期。

---

## 第三轮新增（2026-05-06）

> 客户硬约束要求"务必要实现 Tools、MCP 和 Skill 配置"。本批次 OQ-016 ~ OQ-029 由本轮访谈一次性提出与关闭（OQ-019 除外）；同时 [OQ-001](#oq-001-mvp-切分7-项能力先上哪-12-项) 因范围扩大被重新登记，关键决策点见下方。OQ-016 / OQ-017 / OQ-018 留位与第三轮 Skill / Tool / MCP 设计有重叠，已合并到 OQ-022 ~ OQ-025；为避免编号空洞，逐条标记 "并入 OQ-NNN"。

## OQ-016 工具来源（占位 → 并入 OQ-024）

- **状态**：closed 2026-05-06，并入 [OQ-024](#oq-024-tool-来源)。
- **回写**：以 OQ-024 决策 = A 为准（平台内置勾选清单）。

## OQ-017 MCP 来源（占位 → 并入 OQ-025）

- **状态**：closed 2026-05-06，并入 [OQ-025](#oq-025-mcp-来源)。
- **回写**：以 OQ-025 决策 = A 为准（vNext 锁定平台预置清单）。

## OQ-018 Skill 来源（占位 → 并入 OQ-022）

- **状态**：closed 2026-05-06，并入 [OQ-022](#oq-022-skill-来源)。
- **回写**：以 OQ-022 决策 = A 为准（平台预置 + 用户在线编辑 / 上传 SKILL.md）。

## OQ-019 知识库是否进入本特性

pending

- **问题**：客户在第三轮访谈中没有强制要求知识库进 MVP；REQ-006 是否完全不做、是否 vNext、还是替换为别的形态，留待后续访谈。
- **为什么需要答**：决定 REQ-006 / DB-005 在下一轮立项时的具体形态。
- **影响范围**：REQ-006、DB-005、ND-013（间接）。
- **候选答**：
  - A. 本特性内不做，等下一立项。
  - B. vNext 阶段在本特性内继续 hold 住，等明确文件格式 / 大小限制后再做。
  - C. 替换为 Skill body 模式（已有 SKILL.md 可承载小文档），知识库不另立。
- **卡点等级**：pending（不阻塞 MVP 上线）。
- **回写**：requirements.md REQ-006 / DB-005 标 vNext / pending；本期不做。

## OQ-020 Tool / MCP 计费与首次启用合规告知

A1 + B1

- **问题**：Tool 调用产生的 token 是否与模型调用合并计费？首次启用会外发数据的 Tool / MCP 是否需要告知用户？
- **为什么需要答**：决定 REQ-011 / NFR-005 的可机械验收边界，以及 E6 是否存在。
- **影响范围**：REQ-011、REQ-012、NFR-005、E6、R8。
- **候选答**：
  - A1. Tool / MCP token 合并计入主计费桶。
  - A2. Tool / MCP token 单独配额，按工具计费。
  - B1. 首次启用会外发的 Tool 弹"用户须知"，须确认才启用。
  - B2. 不弹告知，仅在隐私政策中说明。
- **卡点等级**：closed 2026-05-06
- **回写**：requirements.md NFR-005、AC-011-3、AC-011-4、E6。

## OQ-021 客户硬约束：将 Skill / Tool / MCP 从 vNext 提升到 MVP，并接受交付周期调整

R-Accept

- **问题**：客户表态"务必要实现 Tools、MCP 和 Skill 配置"，与原 OQ-001 = A 收敛的 MVP（仅 REQ-001 ~ REQ-004）不一致。是否接受范围扩大、并接受交付周期延长？
- **为什么需要答**：决定 OQ-001 是否 re-open、风险 R1 状态变更。
- **影响范围**：OQ-001、REQ-006 拆分为 REQ-010 / REQ-011 / REQ-012、风险 R1。
- **候选答**：
  - R-Accept. 接受范围扩大并接受交付周期调整。
  - R-Decline. 维持原 MVP，明确告知客户该批能力延后。
- **卡点等级**：closed 2026-05-06
- **回写**：requirements.md 第 5 节新增 REQ-010 ~ REQ-012；R1 升级；OQ-001 re-opened 注脚。

## OQ-022 Skill 来源

A

- **问题**：Skill 来源限定为哪几种？
- **为什么需要答**：决定 REQ-010 的入口数量与 ND 边界。
- **影响范围**：REQ-010、ND-011、ND-012、AC-010-9。
- **候选答**：
  - A. 平台预置 + 用户在线编辑 / 上传单个 SKILL.md；不接 Git URL；不接 ZIP。
  - B. 在 A 基础上加 Git URL 拉取。
  - C. 在 A 基础上加 ZIP / 文件夹上传。
  - D. 全开放。
- **卡点等级**：closed 2026-05-06
- **回写**：requirements.md REQ-010、ND-011、ND-012。

## OQ-023 Skill 格式标准

S1

- **问题**：Skill 文件格式遵循哪个标准？
- **为什么需要答**：决定 REQ-010 的解析器 / 校验器实现路径。
- **影响范围**：REQ-010 全部 AC、E7。
- **候选答**：
  - S1. 遵循 [agentskills.io 标准](https://agentskills.io/specification)（YAML frontmatter `name` / `description` 必填 + Markdown body）。
  - S2. 自定义平台 schema。
  - S3. 兼容 OpenAI Custom GPT instructions / 其他主流形态。
- **卡点等级**：closed 2026-05-06
- **回写**：requirements.md REQ-010 全条、AC-010-1 / AC-010-3、E7。

## OQ-024 Tool 来源

A

- **问题**：Tool 来源限定为哪几种？
- **为什么需要答**：决定 REQ-011 / ND-013 的边界。
- **影响范围**：REQ-011、ND-013、NFR-001。
- **候选答**：
  - A. 仅平台内置勾选清单，不允许用户自填 URL，不走 MCP 协议。
  - B. 允许用户填 URL，按统一签名规范集成。
  - C. 走 MCP 协议（即 Tool = MCP server）。
- **卡点等级**：closed 2026-05-06
- **回写**：requirements.md REQ-011、ND-013、NFR-001。

## OQ-025 MCP 来源

A

- **问题**：MCP server 来源限定为哪几种？
- **为什么需要答**：决定 REQ-012 的 vNext 启用形态。
- **影响范围**：REQ-012、ND-010。
- **候选答**：
  - A. 平台预置清单（vNext 锁定项）。
  - B. 用户自填 URL + token。
  - C. 全开放（含 OAuth2 / API key 输入）。
- **卡点等级**：closed 2026-05-06（vNext 锁定项；vNext 启用时如需重启需重走关闭流程）
- **回写**：requirements.md REQ-012、ND-010、AC-012-3。

## OQ-026 范围风险评估（合并到 OQ-021）

- **状态**：closed 2026-05-06，并入 [OQ-021](#oq-021-客户硬约束将-skill--tool--mcp-从-vnext-提升到-mvp并接受交付周期调整)。
- **回写**：以 OQ-021 = R-Accept 为准；R1 升级登记。

## OQ-027 Skill 编辑 UI 形态

A

- **问题**：Skill 编辑 UI 是否暴露 YAML 语法？
- **为什么需要答**：决定 AC-010-2 与 NFR-001 是否对 Skill 编辑生效。
- **影响范围**：REQ-010 / AC-010-2、NFR-001。
- **候选答**：
  - A. 严格按规范的表单：UI 把 frontmatter 拆成独立字段输入框（`name` / `description` / `license` / `compatibility` / `metadata` 键值编辑器）+ Markdown body 编辑器；用户不暴露 YAML。
  - B. Monaco / CodeMirror 编辑器直接编辑 SKILL.md 全文。
  - C. 两者并存，按 tab 切换。
- **卡点等级**：closed 2026-05-06
- **回写**：requirements.md AC-010-2、NFR-001。

## OQ-028 Skill 重名处理与 Skill ↔ Agent 关联模型

D2 + F3

- **问题**：导入 SKILL.md 时与已有 Skill 重名，怎么处理？Skill 与 Agent 怎么关联？
- **为什么需要答**：决定 REQ-010 的 AC-010-4 与 AC-010-5、DB-006。
- **影响范围**：REQ-010、DB-006。
- **候选答（重名处理 D 系列）**：
  - D1. 直接拒绝。
  - D2. 让用户选"覆盖现有"或"另存为新名称"。
  - D3. 自动追加版本号 -v2。
- **候选答（关联模型 F 系列）**：
  - F1. Agent 内嵌完整 SKILL.md 副本（拷贝模式）。
  - F2. Agent 内嵌 SKILL.md 引用 + 当前快照（混合模式）。
  - F3. 用户级 Skill 库存原本，Agent 上只存"已启用 Skill ID + 当前版本号"的引用（双层模型）。
- **卡点等级**：closed 2026-05-06
- **回写**：requirements.md AC-010-4、AC-010-5、DB-006。

## OQ-029 内置 Tool 首批清单

T-1 + T-3

- **问题**：MVP 首批要内置哪些 Tool？
- **为什么需要答**：决定 REQ-011 / AC-011-1 与 ND-013 边界。
- **影响范围**：REQ-011、ND-013、E6（首次启用是否会外发的判断）。
- **候选答（多选）**：
  - T-1. 联网搜索（外发）。
  - T-2. 计算器（不外发）。
  - T-3. 当前日期 / 时间（不外发）。
  - T-4. URL 抓取。
  - T-5. 图片生成。
  - T-6. 文件读取。
  - T-7. 代码执行。
  - T-8. 表格 / 图表。
- **卡点等级**：closed 2026-05-06
- **回写**：requirements.md AC-011-1（首批 = T-1 + T-3）、ND-013。

## OQ-030 P2 试运行下半屏的落地形态

待 H2 决策

- **问题**：H1 评审会（2026-05-06）反馈：试运行下半屏未来可能改为「Inkwell 平台已有聊天页面 + 通过 iframe 接入 P2」，以避免在本特性内重复实现一套对话 UI、并复用平台的会话 / 历史 / 工具调用渲染等基础能力。本期先按"同页 React 组件"画原型。
- **为什么需要答**：决定 H2 前端选型时是否预留 iframe `postMessage` 桥接、Agent 调用上下文（未保存的 draft `Instructions` / 已勾选 Skill / Tool）如何传给子页、计费桶（NFR-005）如何归并、对话历史是否回写到 P2、网络断开 / E1 重试等状态如何在父子页之间联动。同时影响 H3 详细设计（接口 + 消息协议）。
- **影响范围**：REQ-004、AC-004-1 ~ AC-004-5、ui-spec.md 第 2.2 节"试运行"小节、H2 前端架构选型、H3 P2 ↔ 试运行子页之间的契约。
- **候选答**：
  - A. **同页 React 组件**（当前原型形态）：直接复用平台聊天组件作为 P2 的内嵌区域。
  - B. **iframe 接入独立子页**：试运行复用平台已有完整聊天页面，`<iframe src="/chat?agent=draft&...">`；通过 `postMessage` 同步 draft Instructions 与 Skill / Tool 启用项，及失败 / 重试 / 断网状态。
  - C. **本期 A，vNext 切 B**：先用同页组件交付，待平台聊天页面稳定后切 iframe；切换时不破坏 UI 交互模型。
- **卡点等级**：non-blocking（H1 不卡——UI 交互模型在两种实现下都成立；H2 选型前必须收敛）
- **回写**：H2 阶段（架构）正式选定 A / B / C 之一，补回 ui-spec.md 第 2.2 节"未来实施形态"备注、并在 H3 详细设计里产出对应契约。

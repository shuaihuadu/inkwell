---
id: database-design
title: <项目名称> 数据库设计汇总
stage: H3
status: draft
reviewers: []
upstream:
  - ADR-<NNN>
  - HD-<NNN>
downstream: []
---

> 本文件由 H3 起草过的所有持久化相关模块**累加贡献**——每个模块的 HD 在自己对应章节同步追加一级章节。**只追加**，禁止删除或改动其他模块章节。
>
> 全文最终在所有持久化 HD 起草完毕后由人工评审统一翻 `status: reviewed`。

## 总体设计原则（`ADR-NNN`）

- **Provider 切换**：<列出可切换的数据库 Provider 及选择方式>
- **最小公倍数 schema**：<跨 Provider 类型兼容策略，如禁用 Provider-specific 类型>
- **主键策略**：<主键生成方式>
- **命名约定**：<列命名 / 表命名规则>
- **时间戳字段**：<统一的创建/更新时间字段约定及自动填充机制>
- **乐观并发**：<并发控制字段约定>
- **软删除策略**：<是否支持软删除及理由>
- **enum 存储**：<枚举列的存储约定>
- **Migration**：<每 Provider 的 Migration 组织方式>
- **Seed**：<初始化数据的触发时机与幂等要求>

## `<端口层项目>`（Persistence 端口）

> 由 `HD-NNN`（`<端口层项目>`）锁定。**本节是端口契约**，**不**包含具体表结构（具体表结构由实现层 HD + 各业务 HD 起草）。

### 抽象类型（不入数据库）

`<端口层路径>` 子目录定义的是**端口契约**，不直接对应数据库对象：

- **`<接口/mixin 名>`** — <职责一句话>

### 公共字段映射约定（实现层 HD 锁定）

- **`<类型> <字段名>`** → DB 列名 `<列名>`
  - <各 Provider 对应的物理类型>
  - <约束说明>

### 表清单（占位，由实现层 HD + 业务 HD 锁定）

> 以下表清单来自架构文档已锁定的**业务范围**。每张表的具体字段 / 索引 / 约束由对应业务命名空间 HD 起草，本节仅占位。

| 表名 | 业务模块 | 锁定 HD | 说明 |
| ---- | -------- | ------- | ---- |
|      |          | TBD     |      |

### 错误处理

<统一的数据访问错误处理约定，引用对应 ADR/HD 的详细对照表出处，避免多点重复锁定。>

## `<实现层项目>`（<实现方式> 实现）

> 由 `HD-NNN`（`<实现层项目>`）锁定。**本节是实现锚点**——具体表结构仍由对应业务命名空间 HD 各自起草后增量贡献。

### Mixin 自动配置规则

| Mixin | 行为 | 字段配置 |
| ----- | ---- | -------- |
|       |      |          |

### 拦截器 / 自动填充行为

<描述实现层对时间戳/并发字段/所有权字段的自动填充逻辑，及时钟来源等可测试性设计。>

### Seed 幂等模式

<描述 Seed 的判定键选择原则（禁止用自增/时序主键判定）与失败处理策略。>

### 跨 Provider 字段映射策略

- <逐条列出跨 Provider 兼容性约定，与总体设计原则保持一致，避免漂移>

<!-- 以下由各业务命名空间 HD 在自己的 §5「数据库设计增量」完成后，追加对应的一级章节，格式：
## <业务命名空间>

> 由 [HD-<NNN>](<路径>) 锁定。

- 表名 `<table_name>`
- 列：...
- 索引：...
- Entity / Mapping / Repository 物理位置：...
-->

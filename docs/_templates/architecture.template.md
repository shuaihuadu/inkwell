---
id: architecture-<slug>
stage: H2
status: draft
authors:
  - name: <姓名或 Agent 名>
    role: <owner|agent>
reviewers: []
created: <YYYY-MM-DD>
updated: <YYYY-MM-DD>
upstream:
  - REQ-<slug>
  - repo-impact-map-<slug>
  - tech-selection-<slug>
  - risk-analysis-<slug>
downstream: []
---

# <项目 / 功能名称> · 架构说明

> 本文档必填章节：总体架构图 / 前端 / 后端 / 数据库 / 缓存 / 消息 / 鉴权 / 文件存储 / 部署 / 可观测性 / 性能目标 / 扩展性 / 安全 / 主要技术风险 / 替代方案比较。详细论证见同目录 `adr/` 与 `tech-selection.md` / `risk-analysis.md`；本文是把它们串成一张架构地图，不重复展开选型论证过程。

## 0. 输入与边界

- **输入**：<列出本文档依赖的上游 REQ / NFR / UI / UF / EX 编号区间 + repo-impact-map 章节。>
- **范围裁剪签字**：<引用锁定 v1 范围的 OQ 编号出处。>
- **不在 vX 范围**：<列出明确排除在架构设计之外的能力，避免后续设计"顺手做了"。>

## 1. 总体架构图

<用 Mermaid 画出整体拓扑：客户端层 / 后端服务层 / 业务模块层 / 端口层 / Provider 层 / 数据存储层 / 横切关注点（可观测性等）/ 外部依赖，每层标注对应的 ADR 编号。图后用 bullet 补充图中关键设计原则（如分层依赖方向、隔离边界）。>

```mermaid
<架构图>
```

图说：

- <关键设计原则 1，引用对应 ADR>
- <关键设计原则 2，引用对应 ADR>

## 2. 前端架构

详见 `ADR-NNN`。

- **<层次 1>**：<技术栈 + 职责一句话>
- **<层次 2>**：<技术栈 + 职责一句话>
- **构建**：<构建工具链 + 目标平台矩阵>

## 3. 后端架构

详见 `ADR-NNN`。

### 3.1 物理拓扑（引用锁定拓扑的 `ADR-NNN`）

```text
<项目 / 服务物理目录树，标注每个节点对应的职责与依赖方向>
```

### 3.2 模块说明

- **`<模块 A>`**：<职责 + 依赖约束一句话，引用对应 ADR>
- **`<模块 B>`**：<职责 + 依赖约束一句话，引用对应 ADR>

### 3.3 数据访问

- <ORM / 数据访问框架选型 + Migration 策略 + 本地开发环境策略>
- <抽象接口位置 + 唯一实现位置>
- <Seed / 初始化数据策略>

### 3.4 Builder DSL / 装配方式（如适用）

```<language>
<DI 装配示例代码>
```

## 4. 数据库选型

详见 `ADR-NNN`。

- **关系数据**：<数据库产品 + 版本 + Provider 切换机制>
- **向量数据**（如适用）：<向量库产品 + SDK>
- **关键表**：<按业务模块列出预期的核心表，逐条引用对应 REQ/NFR>
- **Migration**：<每 Provider 一份 Migration 的组织方式>

## 5. 缓存策略

详见 `ADR-NNN`。

- **抽象**：<接口名 + 核心方法>
- **实现**：<v1 唯一实现 + 测试用简化实现>
- **部署**：<dev / prod 部署形态>
- **使用场景**：<逐条列出会用到缓存的业务场景>
- **Key 命名约定**：<约定格式>
- **不在 vX 范围**：<明确排除的缓存能力，如集群 / HA>

## 6. 消息机制

- **客户端↔后端**：<通信协议 + 实时性方案>
- **后端内队列抽象**：<接口名 + 环境对称 Provider 组合（dev / prod）>
- **后端持久工作流**（如适用）：<工作流引擎 + 覆盖场景>
- **不引入外部中间件的理由**（如适用）：<说明为何不引入独立消息中间件>

## 7. 鉴权与权限模型

- **客户端会话**：<会话机制 + 引用对应 NFR>
- **对外 API 鉴权**（如适用）：<机制 + 限流策略>
- **凭据保护**：<凭据存储方式 + dev/prod 差异 + 引用对应 OQ/RISK>
- **不在 vX 范围**：<明确排除的鉴权能力，如 RBAC / SSO>

## 8. 文件存储方案（如适用）

详见 `ADR-NNN`。

- **抽象**：<接口名 + 核心方法>
- **Provider 切换**：<逐个列出 Provider 及各自适用场景>
- **启动选择**：<配置项位置>
- **容器 / 目录命名**：<约定>

## 9. 部署方式

详见 `ADR-NNN`。

### 9.1 dev 部署

```text
<本地开发环境的容器编排清单>
```

### 9.2 prod 部署

```text
<生产环境部署拓扑>
```

- **CI/CD**：<流水线工具 + 测试框架矩阵>
- **region**：<单/多 region 策略>

## 10. 可观测性方案

详见 `ADR-NNN`。

- **三件套**：<Trace / Log / Metric 各自的技术选型>
- **应用 instrumentation**：<SDK / 框架内置能力>
- **关键指标**：<逐条列出核心业务指标>
- **告警**：<告警渠道 + 触发条件>

## 11. 性能目标

> <引用锁定"软目标 vs 硬 SLA"决策的 OQ 编号出处。>

| 维度 | 软目标（P95） | 备注 |
| ---- | ------------- | ---- |
|      |               |      |

## 12. 扩展性设计

- **横向扩展**：<扩缩容机制>
- **数据扩展**：<单实例 vs 分片 / 读副本策略，及 v2 演进方向>
- **可观测性扩展**：<技术栈可扩展性说明>
- **模型 / 第三方能力扩展**（如适用）：<接入新供应商的扩展点>

## 13. 安全设计

详见 `NFR-NNN` 与相关 ADR。

- **传输加密**：<方案>
- **凭据管理**：<方案 + 引用对应 OQ/RISK>
- **客户端安全边界**（如适用）：<IPC 隔离 / 锁屏等>
- **执行边界**（如适用）：<脚本执行限制等>
- **限流**：<方案>

## 14. 主要技术风险

详见 [risk-analysis.md](./risk-analysis.md)。RISK-NNN 摘要：

- [RISK-001](./risk-analysis.md#risk-001-<slug>) <风险标题一句话概述>

## 15. 替代方案比较

详见 [tech-selection.md](./tech-selection.md) 备选项打分表。摘要：

- **<决策维度 1>**：<最终选型完胜候选项的一句话理由>
- **<决策维度 2>**：<最终选型完胜候选项的一句话理由>

## 16. 自检

- 必填章节：总体架构图 / 前端 / 后端 / 数据库 / 缓存 / 消息 / 鉴权 / 文件存储 / 部署 / 可观测性 / 性能目标 / 扩展性 / 安全 / 主要技术风险 / 替代方案比较 —— 完成数 / 15。
- 与 [tech-selection.md](./tech-selection.md) 选型数 + [risk-analysis.md](./risk-analysis.md) 风险数全部交叉引用。
- 引用 ADR / OQ 数目 + REQ / NFR / UI / UF / EX 多条，可向上游追溯。

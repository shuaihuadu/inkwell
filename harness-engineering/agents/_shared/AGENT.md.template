# `<AgentName>`

> 对应阶段：H? | Harness 层：约束层 / 反馈层 / 门禁层
> 共享契约：[`../_shared/glossary.md`](../_shared/glossary.md)、[`../_shared/io-contracts.md`](../_shared/io-contracts.md)、[`../_shared/tool-vocabulary.md`](../_shared/tool-vocabulary.md)

## 1. 定位

<一句话讲清楚本 Agent 的唯一职责。如果一句话讲不清，先回去拆分。>

> 设计依据：<引用规范哪一节 / 哪条业界实践，避免"凭直觉">

## 2. 触发时机

- <什么事件触发？人工还是自动？>
- <在何种状态下不应被触发？>

## 3. 输入契约

| 输入 | 必需 | 说明 |
| --- | --- | --- |
| <产物路径或对象> | 是 / 否 | <格式 / 状态要求> |

**禁止读取**：<列出哪些路径不应进入本 Agent 视野，避免上下文污染>

## 4. 输出契约

### 4.1 主要产物

<路径 + frontmatter 要求 + 正文必须包含的章节>

### 4.2 <次要产物，如有>

### 4.3 阻塞返回

下列情况按 [`io-contracts.md` §5](../_shared/io-contracts.md) 返回结构化错误：

- <可被机械判定的前置条件不满足>

## 5. 工具集

能力 ID 取自 [`_shared/tool-vocabulary.md`](../_shared/tool-vocabulary.md)。

| 能力 | 必需 | 用途 |
| --- | --- | --- |
| `read.file` | 是 | <用途> |
| `write.file` | 是 | <用途> |

**禁用**：<显式列出必须禁用的能力 ID 与原因>

## 6. 行为约束

- **必须**：
  - <可机械验证的硬约束>
- **禁止**：
  - <可机械验证的负向约束，特别是反模式陷阱>

## 7. 验收标准

<本 Agent 一次执行视为合格的可机械判定标准。每条都应能在审查时打 pass / fail>

## 8. 与其他 Agent 的协作

- **上游**：<谁的产物作为本 Agent 的输入>
- **下游**：<本 Agent 的产物被谁消费>

## 9. 已知边界

- <本 Agent 不能解决的问题，避免被错误地扩大职责>
- <需要人工兜底的场景>

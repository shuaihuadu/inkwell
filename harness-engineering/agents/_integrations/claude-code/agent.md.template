---
name: {{AGENT_NAME}}
description: {{ONE_LINER}}
tools: {{TOOL_LIST}}
model: inherit
---

@{{AGENT_DIR}}/prompt.md

## 输入输出契约

参见 @{{AGENT_DIR}}/AGENT.md。本工具包装不重复其内容，仅控制：

- Claude Code 默认权限（见仓库根 `.claude/settings.json`）
- 本 Agent 是否进入 Auto Mode 白名单（默认否）

## 落地清单

- [ ] 已替换全部 `{{...}}` 占位符
- [ ] `tools` 字段与 `AGENT.md` §工具集 一致
- [ ] 已在 `.claude/settings.json` 或同级 settings 中确认权限范围
- [ ] 已在仓库根 `AGENTS.md` 引用本 Agent 的触发约定
